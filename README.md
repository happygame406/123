# 🤖 Чат-бот поддержки

## 🚀 Основные возможности

- **🤖 Умный чат-бот** с предопределенными ответами
- **👤 Система аутентификации** (регистрация/вход)
- **💾 Сохранение истории сообщений** в БД
- **🔄 Множественные сессии** для каждого пользователя
- **⚡ Реальное время** с индикатором набора текста
- **📱 Адаптивный дизайн** для всех устройств
- **🔒 JWT токены** для безопасности
- **📊 Интеграция с БД SQLite** через SQLAlchemy

## 🏗️ Архитектура проекта

```
chat_bot_project/
├── app/                    # Бэкенд FastAPI
│   ├── auth/              # Модуль аутентификации
│   │   ├── login.py       # Логин пользователя
│   │   └── register.py    # Регистрация пользователя
│   ├── bot_logic.py       # Логика ответов бота
│   ├── chat.py            # API для чата и сессий
│   ├── database.py        # Настройка базы данных
│   ├── main.py            # Главное приложение FastAPI
│   ├── models.py          # SQLAlchemy модели
│   ├── schemas.py         # Pydantic схемы
│   └── __init__.py
├── frontend/              # Веб-интерфейс
│   ├── index.html         # Главная страница
│   ├── styles.css         # Стили
│   ├── app.js             # Основная логика фронтенда
│   └── api.js             # API клиент
├── alembic/               # Миграции базы данных
├── .vscode/
│   └── settings.json      # Настройки VS Code
├── requirements.txt       # Зависимости Python
├── run.py                # Скрипт запуска
└── README.md
```

## 🛠️ Технологии

### Бэкенд
- **FastAPI** — современный веб-фреймворк
- **SQLAlchemy** — ORM для работы с БД
- **SQLite** — база данных
- **JWT** — аутентификация
- **Pydantic** — валидация данных
- **Alembic** — миграции БД

### Фронтенд
- **Vanilla JavaScript (ES6 модули)**
- **CSS3** с кастомными свойствами
- **HTML5** семантическая разметка
- **Font Awesome** — иконки

### Инструменты
- **Uvicorn** — ASGI сервер
- **Poetry/Pip** — управление зависимостями

## 📦 Установка и запуск

### 1. Клонирование и настройка

```
git clone <репозиторий>
cd chat_bot_project
```

### 2. Установка зависимостей

```
pip install -r requirements.txt
```

### 3. Запуск приложения

```
python run.py
```

Или напрямую:

```
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

## 🌐 Доступные эндпоинты

### API
- `GET /` — веб-интерфейс
- `GET /docs` — автоматическая документация Swagger
- `GET /health` — проверка работоспособности

### Аутентификация
- `POST /auth/register` — регистрация пользователя
- `POST /auth/login` — вход в систему

### Чат
- `POST /chat/session` — создание новой сессии
- `POST /chat/message` — отправка сообщения
- `GET /chat/history/{session_id}` — история сообщений

## 🔧 Настройка базы данных

Проект использует SQLite с автоматическим созданием таблиц. При первом запуске:

- Создается файл `chatbot.db` в папке `app/`
- Автоматически генерируются таблицы:
  - `users` — пользователи
  - `sessions` — сессии чата
  - `messages` — сообщения

## 🤖 Логика бота

Бот отвечает на следующие фразы:

- `привет` — приветствие
- `как дела` — состояние бота
- `время` — текущее время
- `помощь` — справка
- `спасибо` — благодарность
- `пока`/`бб` — прощание
- `123` — возвращает "123"

## 🔐 Безопасность

- Пароли хэшируются с использованием **bcrypt**
- **JWT токены** с истечением через 30 минут
- Валидация входных данных на всех уровнях
- Защита от SQL-инъекций через ORM

## 📱 Интерфейс пользователя

### Основные элементы:
- **Заголовок** — название и кнопки авторизации
- **Окно чата** — история сообщений
- **Панель ввода** — отправка новых сообщений
- **Боковая панель** — история сессий
- **Модальные окна** — регистрация/авторизация

## 📄 Лицензия

MIT License
